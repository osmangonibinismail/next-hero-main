import React from 'react'

export const metadata = {
    title: 'Login',
    description: "Login Page"
}
const page = () => {
    return (
        <div>
            this is login page
        </div>
    )
}

export default page
